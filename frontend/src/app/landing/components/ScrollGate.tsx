'use client';

/**
 * Re-exports from scrolling.tsx so the whole landing scroll is operated there.
 * Use ScrollGate and useScrollGate from here or from ./scrolling.
 */
export { ScrollGate, useScrollGate } from './scrolling';
